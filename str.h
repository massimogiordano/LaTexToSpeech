#ifndef STR_H
#define STR_H

#include <str.h>
#include <iostream>
using namespace std;

string add_back_slash(string input);
string inside_quotes(string &text);
string delSpaces(string &str);

#endif

